import styled from "styled-components";

const Spacing = styled.p `
  white-space: pre-wrap;
  word-wrap: break-word;
`;

const Cont = styled.div `
    margin-left: 80px;
`;

const black = styled.span `
    background-color: black;
    color: white;
`;

export default { Spacing, Cont, black };